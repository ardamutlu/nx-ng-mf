{
  "extends": ["../../.stylelintrc.json"],
  "overrides": [
    {
      "files": ["**/*.css", "**/*.scss"],
      "rules": {
        "at-rule-no-unknown": [
          true,
          {
            "ignoreAtRules": ["nxgraphsource"]
          }
        ]
      }
    }
  ]
}
